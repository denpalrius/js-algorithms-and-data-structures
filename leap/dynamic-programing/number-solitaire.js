        // // write your code in C# 6.0 with .NET 4.5 (Mono)

        // //create a marked array
        // int[] maxvalues = new int[A.Length];
        // A[0] = 1;
        // for(int i =1; i < A.Length; i++){
        //     //int val1 = i;
        //     int maxval = int.MinValue;
        //     for(int dice= i; dice >=0; dice--){
        //         //Console.WriteLine(Math.Abs(dice-i));
        //         if(Math.Abs(dice-i) > 0 && dice <= 6){
        //             maxval = Math.Max(maxval, maxvalues[i-dice]+A[i]);

        //         }   
        //     }
        //     maxvalues[i] = maxval;
        // }
        // return maxvalues[A.Length-1];